Repo is clean mechanically (no whitespace/newline/link issues) but README.md is stale against the actual plugin set. No auto-fixes applied - both issues change meaning, so they're proposals only.

## Auto-applied
none

## Proposals

1. README.md is out of date on which plugins exist - `README.md:9,15-19`
   - What: install block runs `claude plugin install pretext-typography@gkmur` and documents a `### pretext-typography` section. CLAUDE.md:26-27 says `pretext-typography` was deleted 2026-08-14, never installed, never invoked. README also never mentions `adhd` or `maintainer-orchestrator`, both of which are live in `.claude-plugin/marketplace.json` and have working plugin.json + SKILL.md.
   - Why: anyone using README.md as the install doc will try to install a plugin that doesn't exist and miss the two that do.
   - Risk of leaving as-is: low urgency but actively misleading for a first-time reader (or the cloud/consumer path the marketplace exists for).

2. `maintainer-orchestrator` has two different descriptions - `.claude-plugin/marketplace.json:42` vs `plugins/maintainer-orchestrator/.claude-plugin/plugin.json:2`
   - What: marketplace.json still describes it as "wakes on a heartbeat... directs one background worker agent per repo... Claude Code port of steipete's Codex maintainer loop" (the old multi-repo orchestrator). The plugin's own plugin.json says that multi-repo loop "was removed on 2026-08-13; this plugin is now the per-repo triage skill only."
   - Why: the marketplace listing (what a consumer sees before installing) advertises capability the plugin no longer has.
   - Risk of leaving as-is: moderate - sets wrong expectations for anyone installing off the marketplace listing.
