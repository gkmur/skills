Repo is mechanically clean - no broken links, no trailing whitespace, no missing newlines, no dupe blocks. Same two open items as every brief since 06-22/07-26, still unactioned, no new drift found.

## Auto-applied
none

## Proposals
1. **(carried over since 2026-07-26) `.claude-plugin/marketplace.json`'s maintainer-orchestrator description still describes the removed multi-repo orchestrator.** `.claude-plugin/marketplace.json:26` says "Run a single orchestrator session that wakes on a heartbeat... directs one background worker agent per repo... Claude Code port of steipete's Codex maintainer loop: /loop 5m heartbeat..." - that feature was removed 2026-08-13 (commit 11ebc62). `plugins/maintainer-orchestrator/.claude-plugin/plugin.json`'s description is already accurate. `marketplace.json`'s `keywords` array also still lists `orchestrator`, `loop`, `background-agents`, `autonomous`, `steipete`. Fix is a straight copy of plugin.json's description/keywords into marketplace.json - low risk, but it's a marketplace-facing description change so left for you.

2. **(carried over since 2026-06-22, still open) README.md is stale against the actual plugin lineup.** `README.md:9,15-19` still lists `pretext-typography` (deleted 2026-08-14) as an installable plugin with its own section, and the Install/Plugins sections never mention `adhd` or `maintainer-orchestrator` (both live since 2026-06-23/06-11). `.claude-plugin/marketplace.json` has accurate one-line descriptions for all 3 current plugins - straightforward copy-source. Flagged in every brief since 06-22, never actioned.
